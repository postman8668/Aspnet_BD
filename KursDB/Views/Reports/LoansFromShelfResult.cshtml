@using KursDB.Models.ViewModels
@model List<LoanFromShelfViewModel>

<h2>Результаты поиска выдач с полки</h2>

@if (Model != null && Model.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>Название издания</th>
                <th>ФИО читателя</th>
                <th>Дата выдачи</th>
                <th>Срок возврата</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.PublicationTitle</td>
                    <td>@item.ReaderName</td>
                    <td>@item.LoanDate.ToShortDateString()</td>
                    <td>@item.DueDate.ToShortDateString()</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>Выдачи с указанной полки не найдены.</p>
}

<a asp-action="LoansFromShelf" class="btn btn-secondary">Назад</a>