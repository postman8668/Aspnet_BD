@model IEnumerable<KursDB.Models.HallType>

@{
    ViewData["Title"] = "Типы залов";
}

<h1>Типы залов</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<table id="hallTypeTable" class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.TypeName)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.TypeName)</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.HallTypeId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.HallTypeId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.HallTypeId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.datatables.net/1.10.21/js/jquery.dataTables.min.js"></script>
    <script>
        $(document).ready(function() {
            $('#hallTypeTable').DataTable();
        });
    </script>
}
