@model KursDB.Models.Authorship

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div class="alert alert-warning" role="alert">
    <strong>Warning:</strong> Deleting this authorship record will permanently remove all associated data. This action cannot be undone.
</div>
<div>
    <h4>Authorship</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Author
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Author.LastName)
        </dd>
        <dt class="col-sm-2">
            Work
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Work.Title)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="AuthorshipId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>